// Smoke test for SNBT Tracker
import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Just verify the app builds — actual widget tests are separate
    expect(true, isTrue);
  });
}
